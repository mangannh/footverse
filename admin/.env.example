# Base URL of the FootVerse backend API (no trailing slash, no /api/v1 suffix).
# Copy this file to `.env` and set a real value for local development.
# Example for a local backend: http://localhost:8080
VITE_API_BASE_URL=
